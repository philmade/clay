# Compiled binaries (built by Docker, not committed)
/clay
/clay-medic
/clay-bridge
/clay-proxy
/bridge
/proxy
*.db

# Environment (contains API keys)
.env

# Local test artifacts (runtime dirs at repo root — anchored so they don't
# match the committed defaults/ templates)
/soul/
/trends/
/.adk/
/data/

# macOS
.DS_Store
